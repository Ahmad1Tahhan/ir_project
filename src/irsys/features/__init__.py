"""Optional / bonus features (each independently testable).

  * clustering   — KMeans over document embeddings (+ cluster-restricted search)
  * topics       — topic detection via NMF/LDA over TF-IDF
  * rag          — retrieval-augmented generation with a local GPU LLM
"""
